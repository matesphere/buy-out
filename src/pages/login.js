import React from 'react'
import { Redirect } from '@reach/router'

import { withAuthenticator } from '@aws-amplify/ui-react'

const ChooseRoute = (props) => <div>hello</div>

export default withAuthenticator(ChooseRoute)
